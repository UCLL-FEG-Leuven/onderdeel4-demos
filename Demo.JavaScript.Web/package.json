{
  "name": "onderdeel4.demo.javascript",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm install && concurrently \"cd client && npm install && npm run watch\" \"cd server && npm install && npm run serve\""
  },
  "keywords": [
    "UCLL",
    "JavaScript",
    "Fundamenten"
  ],
  "devDependencies": {
    "concurrently": "7.5.0"
  }
}