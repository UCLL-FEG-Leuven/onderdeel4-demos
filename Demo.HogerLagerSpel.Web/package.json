{
  "name": "onderdeel4.demo.hoger-lager-spel",
  "version": "1.0.0",
  "private":  true,
  "description": "",
  "scripts": {
    "start": "npm install && concurrently \"cd client && npm install && npm run watch\" \"cd server && npm install && npm run serve\""  },
  "keywords": [
    "UCLL",
    "hoger",
    "lager"
  ],
  "devDependencies": {
    "concurrently": "7.5.0"
  }
}